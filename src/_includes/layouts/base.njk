<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <title>{{ settings.metaTitle }}</title>
    <meta name="description" content="{{ settings.metaDescription }}"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#3182CE">
    <link rel="stylesheet" href="/assets/css/tailwind.css"/>
    <script src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.4.1/dist/alpine.min.js" defer></script>

    <!-- ======================= -->
    <!-- Fathom stats -->
    {% if settings.stats.fathom -%}
      <script src="https://cdn.usefathom.com/script.js" site="{{ settings.stats.fathom }}" defer></script>
    {% endif -%}
    <!-- end Fathom stats -->

    <!-- ======================= -->
    <!-- Google stats -->
    {% if settings.stats.google -%}
      <!-- Global site tag (gtag.js) - Google Analytics -->
      <script async src="https://www.googletagmanager.com/gtag/js?id={{ settings.stats.google }}"></script>
      <script>
        window.dataLayer = window.dataLayer || [];
        function gtag() {
          dataLayer.push(arguments);
        }
        gtag('js', new Date());

        gtag('config', '{{ settings.stats.google }}');
      </script>
    {% endif -%}
    <!-- end Google stats -->

  </head>
  <body class="{{ settings.colors.background | default('bg-gray-800',true) }}">

    <!-- NAV -->
    {% include "components/nav.njk" %}
    <!-- ==================================================== -->

    <article class="max-w-3xl mx-auto mt-16 sm:mt-16">

      <!-- HERO and SubTitle -->
      {% include "components/hero.njk" %}
      <!-- ==================================================== -->

      <!-- CONTENT -->
      <main>
        <div class="{{ settings.colors.bodyText | default('text-gray-400',true) }}">
          {{ content | safe }}
        </div>
      </main>
      <!-- ==================================================== -->

      <!-- SOCIAL SVGs -->
      {% include "components/social.njk" %}
      <!-- ==================================================== -->

    </article>
  </body>
</html>